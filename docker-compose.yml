version: '3.8'

services:
  tac-voice:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    environment:
      # Override NGROK_BASE_URL if running behind a proxy
      - NGROK_BASE_URL=${NGROK_BASE_URL}
